import './globals.css';
export const metadata={title:'Orbit / Run — A 3D physics playground',description:'Roll through floating gardens, collect gems, and find your balance. A free 3D marble game with real physics and touch controls.'};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
