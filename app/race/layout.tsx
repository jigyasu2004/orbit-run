export const metadata={title:'DustRun — Race the bots',description:'A free 3D arcade racing game. Race three AI drivers around Sunset Circuit, with keyboard and mobile controls.'};
export default function RaceLayout({children}:{children:React.ReactNode}){return children}
